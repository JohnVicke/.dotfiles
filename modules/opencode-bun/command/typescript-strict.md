---
description: Strict TypeScript patterns - no any, no assertions, type-safe code
---

Load the "typescript-strict" skill and enforce its rules.

When writing or reviewing TypeScript code: no `any`, no non-null assertions (`!`), no type assertions (`as Type`). Use discriminated unions, type guards, and parse at boundaries.

$ARGUMENTS
